spring:
  profiles:
    active: dev
    include: secret
  jpa:
    hibernate:
      ddl-auto: create
      jdbc:
        batch_size=50  # ??? DB??? ? ? ?? ?? ?? ??
    properties:
      hibernate:
        show_sql: true
        format_sql: true # SQL?? ?? ??? ??? ?? ??, ?? ??? ?? select * From ~~~ ???
        use_sql_comments: true  # JPQL? ?? ??

logging:
  level:
    root: INFO
    com.wanted.sns_feed_service: DEBUG  #????
    org.hibernate.orm.jdbc.bind: TRACE # SQL ???? ???(?? ??? ?? ??)
    org.hibernate.orm.jdbc.extract: TRACE #??? ??? ???

custom:
  jwt:
    secretKey: